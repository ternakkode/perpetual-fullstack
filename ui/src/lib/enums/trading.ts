export enum PositionType {
  Long = "LONG",
  Short = "SHORT",
}

export enum PairType {
  Base = "Base",
  Quote = "Quote",
}

export enum PositionStatus {
  OPEN = "OPEN",
  CLOSED = "CLOSED",
  PARTIAL = "PARTIAL",
}